"""Placeholder Lambda entry point.

Task 4 replaces this with the real handler. This file exists only so
Terraform's `archive_file` data source (infra/modules/processor-lambda)
has a directory to zip while Task 3 verifies the infrastructure on its
own, before the handler logic exists.
"""


def handle(event, context):
    raise NotImplementedError("Task 4 implements the real handler")
